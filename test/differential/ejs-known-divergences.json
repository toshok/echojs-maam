{
  "//": [
    "echojs-vs-node divergences the ejs lane FOUND and root-caused. Each entry",
    "is a pre-existing echojs runtime/compiler bug verified by a minimal",
    "standalone probe — not a maam machine bug (the concrete-vs-node lane must",
    "be green on the file). Entries are structured (symptom + rootCause, both",
    "required — the harness rejects malformed entries) so the record cannot",
    "rot into prose. Gate semantics: a listed file that diverges reports KNOWN",
    "(non-failing); a divergence NOT listed fails the gate; a listed file that",
    "stops diverging fails the gate as STALE; a listed file that stops",
    "COMPILING (N/A) is reported as unvalidatable — a warning, not a failure,",
    "because N/A means the semantic claim cannot be tested in either direction",
    "(see harness). Tracked in echojs docs/maam-p0-results.md 'Phase 3.5'.",
    "Currently empty: the original seven 2026-07-23 entries (1+null abort,",
    "negative >>> ToUint32, -0 === 0, Math.round ties, Number-string trim,",
    "typeof null) were all fixed by echojs runtime-P1 and removed when the",
    "lane verified them green."
  ]
}
